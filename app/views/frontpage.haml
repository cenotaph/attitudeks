- @sets.in_groups_of(2).each do |set|
  .pure-g
    - set.each do |i|
      - next if i.nil?
      .pure-u-1-1.pure-u-md-1-2.front_image_wrapper
        .front_image= link_to image_tag(i.file.url(:medium), class: "pure-img"), "/step/#{i.imageset.slug}"